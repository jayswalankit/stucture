#include<stdio.h>
#include<string.h>
int main(){
    char str[1000];
    puts("enter your no");
    int size=strlen(str);
fgets(str,1000,stdin);
    for(int i=0;str[i];i++){

    }
    
   puts(str);
    
    
}